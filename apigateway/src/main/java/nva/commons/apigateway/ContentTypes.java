package nva.commons.apigateway;

import java.util.Locale;

public final class ContentTypes {

    public static String APPLICATION_JSON = "application/json".toLowerCase(Locale.ROOT);
    public static String WILDCARD = "*/*".toLowerCase(Locale.ROOT);

    private ContentTypes() {
    }
}
