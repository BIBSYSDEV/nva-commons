plugins {
    id 'nvacommons.root'
    alias(libs.plugins.gradle.versions)
}

repositories {
    gradlePluginPortal()
    mavenCentral()
}

allprojects {
    group = project.property('GROUP')
    version = project.property('VERSION_NAME')
}
